import /*subTwoNum, */{ printConsole, sumTwoNum } from "./util.js";
import {default as subTwoNum} from "./util.js";
function main() {
    printConsole("Tong 2 so: " + sumTwoNum(1,2))
    printConsole("Tong 2 so: " + subTwoNum(1,2))
}

main();